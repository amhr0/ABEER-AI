# AI Agent System - Complete Deployment

نظام وكيل الذكاء الاصطناعي الكامل مع واجهة ويب، API، Docker، ودعم SSL.

## 📋 ملف التوزيع

الملف الرئيسي: `deploy-ai-agent.sh`

## 🚀 طريقة الاستخدام

1. **رفع الملف إلى السيرفر:**
   ```bash
   # من جهازك المحلي
   scp deploy-ai-agent.sh user@your-server.com:~
   ```

2. **تشغيل الملف على السيرفر:**
   ```bash
   # على السيرفر
   chmod +x deploy-ai-agent.sh
   ./deploy-ai-agent.sh
   ```

3. **إعداد SSL (اختياري):**
   ```bash
   ./setup-ssl.sh
   ```

## ⚙️ الإعدادات

قبل التشغيل، قم بتعديل هذه المتغيرات في الملف:

```bash
DOMAIN="abeer.sas-nex.com"           # دومينك
EMAIL="admin@abeer.sas-nex.com"      # بريدك الإلكتروني
OPENAI_API_KEY="YOUR_OPENAI_API_KEY_HERE"  # مفتاح OpenAI API
```

## 🏗️ ما الذي يقوم به الملف؟

- ✅ تحديث النظام وتثبيت المتطلبات
- ✅ تثبيت Docker و Docker Compose
- ✅ إنشاء Backend (Node.js + Express)
- ✅ إنشاء Frontend (HTML/CSS/JS)
- ✅ إعداد Nginx كـ reverse proxy
- ✅ دعم SSL/HTTPS مع Let's Encrypt
- ✅ حماية rate limiting و CORS
- ✅ واجهة دردشة حديثة مع AI

## 🔧 أوامر الإدارة

```bash
# مشاهدة السجلات
docker-compose logs -f

# إيقاف الخدمات
docker-compose down

# إعادة تشغيل الخدمات
docker-compose restart

# إعداد SSL
./setup-ssl.sh
```

## 🌐 روابط الوصول

- **الواجهة الرئيسية:** https://your-domain.com
- **API:** https://your-domain.com/api
- **Health Check:** https://your-domain.com/api/health

## 📁 ملفات النظام

```
~/ai-agent/
├── backend/          # Node.js backend
├── frontend/         # واجهة الويب
├── nginx/            # إعدادات Nginx
├── docker-compose.yml # إعدادات Docker
├── deploy.sh        # سكريبت التوزيع
├── setup-ssl.sh     # سكريبت SSL
└── deployment-info.txt # معلومات التوزيع
```

## 🔒 الأمان

- ✅ Rate limiting للحماية من الهجمات
- ✅ CORS محدد للدومين الخاص بك
- ✅ SSL/TLS تشفير
- ✅ Security headers
- ✅ Input validation

## 🛠️ المتطلبات

- Ubuntu/Debian Linux
- صلاحيات sudo
- اتصال إنترنت
- دومين مشتخر (اختياري لـ SSL)

## 📞 الدعم

إذا واجهت مشاكل:
1. تحقق من السجلات: `docker-compose logs -f`
2. تأكد من أن المنافذ 80 و 443 مفتوحة
3. تحقق من أن الدومين يشير إلى السيرفر
4. تأكد من صحة مفتاح OpenAI API

---

**جاهز للإنتاج! 🚀**