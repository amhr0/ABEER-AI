# ABEER AI - وكيل ذكاء اصطناعي متكامل

وكيل ذكاء اصطناعي متقدم مبني بتقنيات حديثة يوفر قدرات متكاملة للمحادثة، البحث، إدارة السيرفر، وقاعدة المعرفة.

## المميزات الرئيسية

### 🤖 الذكاء الاصطناعي المتقدم
- دعم OpenAI GPT-4 و Google Gemini
- ذاكرة طويلة الأمد للسياق عبر المحادثات
- قاعدة معرفة ذكية لحفظ الوثائق والمعلومات
- البحث في الإنترنت (DuckDuckGo)
- التكامل مع GitHub API

### 💬 نظام المحادثة
- واجهة دردشة حديثة وسريعة
- حفظ سجل المحادثات في قاعدة البيانات
- عرض الأكواد البرمجية بشكل احترافي
- دعم Markdown والتنسيق المتقدم

### 🖥️ لوحة تحكم السيرفر
- تنفيذ أوامر SSH عن بُعد
- مراقبة موارد السيرفر (CPU, Memory, Disk)
- إدارة الملفات (قراءة، كتابة، حذف)
- قائمة بيضاء للأوامر المسموحة

### 🔍 تحليل الأخطاء الذكي
- اكتشاف الأخطاء تلقائياً
- تحليل السجلات واقتراح الحلول
- تصنيف الأخطاء حسب الخطورة
- تتبع حالة الأخطاء (جديد، قيد التحليل، تم الحل)

### 🔐 الأمان والمصادقة
- نظام مصادقة Manus OAuth
- إدارة الصلاحيات والأدوار
- تشفير البيانات الحساسة
- قائمة بيضاء للأوامر الآمنة

## التقنيات المستخدمة

### Frontend
- **React 19** - مكتبة واجهة المستخدم
- **TypeScript** - لغة البرمجة
- **Tailwind CSS 4** - إطار عمل التصميم
- **tRPC** - التواصل الآمن مع الخلفية
- **Wouter** - التوجيه (Routing)
- **Shadcn/ui** - مكونات UI جاهزة

### Backend
- **Node.js 22** - بيئة التشغيل
- **Express 4** - خادم الويب
- **tRPC 11** - API Type-Safe
- **Drizzle ORM** - إدارة قاعدة البيانات
- **MySQL/TiDB** - قاعدة البيانات

### AI & Integration
- **OpenAI API** - GPT-4
- **Google Gemini API** - نموذج بديل
- **DuckDuckGo API** - البحث في الإنترنت
- **GitHub API** - التكامل مع GitHub
- **SSH2** - الاتصال بالسيرفرات

## التثبيت والتشغيل

### المتطلبات
- Node.js 18 أو أحدث
- pnpm (مدير الحزم)
- MySQL أو TiDB (قاعدة البيانات)

### خطوات التثبيت

1. **استنساخ المشروع**
```bash
git clone https://github.com/amhr0/ABEER-AI.git
cd ABEER-AI
```

2. **تثبيت المكتبات**
```bash
pnpm install
```

3. **إعداد متغيرات البيئة**
أنشئ ملف `.env` في جذر المشروع:
```env
# Database
DATABASE_URL=mysql://user:password@host:port/database

# Authentication
JWT_SECRET=your-secret-key
OAUTH_SERVER_URL=https://api.manus.im
VITE_OAUTH_PORTAL_URL=https://portal.manus.im

# AI APIs
OPENAI_API_KEY=sk-...
GEMINI_API_KEY=AIza...

# GitHub Integration
GITHUB_USER=your-username
GITHUB_PAT=ghp_...

# Server Control (Optional)
SSH_HOST=your-server-ip
SSH_PORT=22
SSH_USER=your-ssh-user
SSH_PRIVATE_KEY_CONTENT=-----BEGIN OPENSSH PRIVATE KEY-----...

# App Config
VITE_APP_TITLE=ABEER AI
VITE_APP_ID=your-app-id
OWNER_OPEN_ID=your-owner-id
OWNER_NAME=Your Name
```

4. **إعداد قاعدة البيانات**
```bash
pnpm db:push
```

5. **تشغيل المشروع**
```bash
# Development
pnpm dev

# Production
pnpm build
pnpm start
```

## الاختبارات

```bash
# تشغيل جميع الاختبارات
pnpm test

# تشغيل الاختبارات مع التغطية
pnpm test:coverage
```

## البنية التنظيمية

```
ABEER-AI/
├── client/              # الواجهة الأمامية (React)
│   ├── public/         # الملفات الثابتة
│   └── src/
│       ├── components/ # مكونات UI
│       ├── pages/      # صفحات التطبيق
│       ├── contexts/   # React Contexts
│       └── lib/        # مكتبات مساعدة
├── server/             # الخلفية (Node.js)
│   ├── _core/         # الوظائف الأساسية
│   ├── routers.ts     # tRPC Routers
│   └── db.ts          # دوال قاعدة البيانات
├── drizzle/           # Schema قاعدة البيانات
├── shared/            # أكواد مشتركة
└── tests/             # الاختبارات

```

## المساهمة

نرحب بالمساهمات! يرجى:
1. عمل Fork للمشروع
2. إنشاء فرع جديد (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'Add amazing feature'`)
4. Push للفرع (`git push origin feature/amazing-feature`)
5. فتح Pull Request

## الترخيص

هذا المشروع مرخص تحت رخصة MIT.

## الدعم

للمساعدة أو الإبلاغ عن مشاكل، يرجى فتح Issue في GitHub.

---

**Built with ❤️ using React, TypeScript, and tRPC**
